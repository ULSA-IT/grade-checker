{
  "name": "ulsa-gpa-planner",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "test": "node scripts/test.cjs",
    "check": "node --check domain.js && node --check import.js && node --check score-input.js && node --check app.js && node --check connection.js && node --check onboarding.js"
  }
}
