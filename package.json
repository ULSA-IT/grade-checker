{
  "name": "ulsa-gpa-planner",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "test": "node --test tests",
    "check": "node --check domain.js && node --check import.js && node --check score-input.js && node --check app.js"
  }
}
